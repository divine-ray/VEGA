version: '3.2'
services:
  daemon:
    image: "node:lts-alpine"
    command: "npx nodemon ."
    working_dir: /demonicbot
    environment:
      - NODE_ENV=production
    volumes:
      - ./:/demonicbot
    links:
      - redis
  redis:
    image: "redis:alpine"