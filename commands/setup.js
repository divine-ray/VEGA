module.exports = {
    name: 'setup',
    cooldown: 500,
    description: 'Creates the required roles for a couple features of this bot.',
    execute(message, args) {
        message.channel.send('Setting up Roles..');
        console.log('Setting up Roles..')
        new Discord.Role(clientdataguild);

















    }
}